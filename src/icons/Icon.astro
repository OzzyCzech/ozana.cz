---
export interface Props {
	name: string;
}


const { name, ...attributes } = Astro.props as Props;

const {default: innerHTML} = await import(`../icons/${name}.svg?raw`);
---

<Fragment set:html={innerHTML} {...attributes}/>