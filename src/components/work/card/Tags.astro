<ul class:list={["tags", Astro.props.class]}>
	<slot/>
</ul>

<style>
	@reference "tailwindcss";

	ul {
		@apply flex justify-start items-center flex-wrap gap-2 no-underline hover:no-underline;

		> li {
			@apply px-2 py-1 bg-gray-200 rounded-sm text-sm text-gray-500;
		}
	}
</style>