---
interface Props {
	emoji: string;
	[key: string]: any;
}

const { emoji } = Astro.props;
---

<img
	{...Astro.props}
	src={`https://raw.githubusercontent.com/twitter/twemoji/master/assets/svg/${emoji.codePointAt(0).toString(16)}.svg`}
	alt={emoji}
	draggable="false"/>